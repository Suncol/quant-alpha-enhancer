[project]
name = "quant-alpha-enhancer"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "lightgbm[scikit-learn]>=4.6.0",
    "matplotlib",
    "numpy",
    "pandas",
    "statsmodels",
]

[dependency-groups]
dev = [
    "pytest",
]
